
 #Create a class with property named 'name', 'age' and 'male'

# class Person:
    # name = 'Quyet'
    # age = 32
    # male = True
    
    # Create a method in Person class
    # Methods in object are functions that belong to the object
    
    # def set_name(self, name):
    #     name = self.name
    
    # def get_name(self):
    #     return self.name

    # def set_age(self, age):
    #     self.age = age

    # def get_age(self):
    #     return self.age        

    # def set_male(self, male):
    #     self.male = male
    
    # def get_male(self):
    #     return self.male
    
    # """ The 'self' parameter is a reference to the current instance of the class, 
    # and is used to access variables that belongs to the class
    # It does not have to be named self , you can call it whatever you like, 
    # but it has to be the first parameter of any function in the class """

    # def set_name(my_object, name):
    #     my_object.name = name
    # def get_name(my_object):
    #     return my_object

# Create an Object named person

# person = Person()

# # Print the value of property

# # print(f"Name: {person.name}")
# # print(f"Age: {person.age}")
# # print(f"Male: {person.male}")

# # Execute function on the person object

# person.set_age(30)
# print(person.get_age())

# Create a class named Person, use the __init__ function to assign values for name, age, male


# class Person:
#     def __init__(self, name, age, male):
#         self.name = name
#         self.age = age
#         self.male = male
#     def myfunc(myself):
#         print(f'Hello {myself.name} !')
#         print(f'{myself.age} year old')
# person = Person('Quentin', 32, True)        
# # print(person.name)
# # print(person.age)
# # print(person.male)
# #person.name = 'Quyet'
# # person.age = 20
# del person.name
# person.myfunc()








